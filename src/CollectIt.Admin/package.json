{
  "name": "collectit.admin",
  "version": "1.0.0",
  "description": "Project for Admin page for .NET semester project",
  "main": "index.ts",
  "scripts": {
    "build": "concurrently \"npm run --prefix client build\" \"npm run --prefix server build\"",
    "run": "concurrently \"npm run --prefix client start\" \"npm run --prefix server start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^8.4.3",
    "concurrently": "^7.0.0",
    "react": "^18.0.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "@types/react": "^18.0.5",
    "react-dom": "^18.0.0",
    "react-router-dom": "^6.3.0",
    "typescript": "^4.6.3"
  }
}
